# Literary OS — P07 Preformal Session Close Handoff R2 (2026-09-05)

## 0. Authority of this file
This file supersedes only the *current-state interpretation* of `START_HERE_P07_PREFORMAL_SESSION_CLOSE.md`; the older file remains historical evidence and must not be deleted or rewritten.

This R2 handoff records the latest state reached in the follow-up session before the local CAAS/container/python backend again began returning `ClientError` even for minimal commands. The infrastructure failure is not evidence of Literary OS engine failure.

## 1. Current global authority state
- P06: COMPLETED / PHYSICALLY CLOSED.
- P07: ACTIVE PREFORMAL. Do not call P07 complete.
- Formal scored count: 137.
- Latest formal scored experiment: R138.
- R140 formal attempts / outputs / scores: 0 / 0 / 0.
- ENG:R47 Production: immutable; do not modify.
- DB59 frozen reference snapshot SHA256: `a5cff0fcd43584220f41a4be85b112c7fc5246977d856797d2676546bccb6bc9`.
- Existing six R140 rehearsal/repair works remain DEVELOPMENT SET ONLY and are not eligible as the eventual fresh formal R140 sample.
- P07 is still preformal problem-discovery / repair / parity-qualification work. No formal R140 claim may be made from current preformal outputs.

## 2. Three-stage research lineage that must remain active
### Stage 1 — R001–R103
Scene/Sequence control, retrieval, boundary, episode planning, selector/controller, hierarchy, blueprint, multi-episode rollout and their negative guards / metrology / supersession lineage.

Do not interpret 103 experiments as 103 runtime algorithms. Historical implementation trace classified the early lineage into adopted, conditional, negative-guard, metrology-only and superseded evidence. Successful results may become runtime modules/policies; failures may become guards; metrology-only and superseded work need not become active generation code.

### Stage 2 — PRE-R104-AUX-001–068
Non-formal engineering/metrology layer between R103 and R104: LLM renderer, semantic safety, DB consumption, source-state, runtime wiring, evidence integrity, reproducibility, inspector/validator and related infrastructure. AUX-068 does not add 68 to the formal experiment count.

### Stage 3 — R104 onward
End-to-end system lineage: Source-State -> Entity/Relationship/Thread -> Whole Story -> Episode Allocation -> Ensemble/Ecology -> Episode Synopsis/Plan -> Sequence -> Scene -> Broadcast Scale -> Surface Realization -> State Carry / Repair / Rollback.

Historical headline claims must not override later clean revalidation. Important current boundaries include:
- R129->R134: conflicted / not cleanly replicated.
- R48 clean replay: KNOWLEDGE_ONLY.
- R39 clean replay: fail as clean causal replication despite directional hierarchy signal.
- R41/P01: Ensemble effect reproduced; simple tone add-on not independently supported.
- R42/P02: blueprint depth and mismatch harm reproduced; incremental thread-binding claim failed because consumer fidelity was insufficient.
- R103/P03 and R121/P04: iterated prospective clean engineering revalidation passes; preserve failed attempts.

## 3. Evidence layers — do not collapse them
The following are distinct evidence classes and must never be silently treated as equivalent:

1. **Concept Application** — ChatGPT/researcher understands Literary OS principles and applies them directly in conversation or analysis.
2. **Virtual/Local Engine Rehearsal** — actual code/DB/schema/routing/guards/hashes/state are executed in the container, but not necessarily through the locked live provider.
3. **Live Provider Engine Execution** — the frozen candidate engine calls the locked OpenAI API provider/model/settings and retains provider receipts.
4. **Formal Controlled Evidence** — fresh sample, locked preregistration, blind control/treatment, frozen thresholds, independent judging and formal gates.

R48 is the direct warning against collapsing these layers: concept-prompt knowledge materially outperformed the then-current runtime implementation, so `research principle understood` is not evidence that `engine runtime faithfully consumes the principle`.

## 4. Parity doctrine for P07
P07 must establish parity at multiple layers, not byte-identical prose.

### A. Mechanical / Trace Parity
The virtual rehearsal and live engine execution must preserve the same frozen source/DB snapshot, source cutoff, runtime tree, prompt serialization policy, research-policy hashes, schemas, retrieval policy, guard thresholds, stage order and input contracts where applicable.

### B. Consumer Parity
A principle counted as active only when concrete values flow through a real consumer and alter downstream behavior with traceable receipt/evidence. `Field exists` or `hash logged` is insufficient.

Required pattern:
`Value -> Consumer -> Behavior Change -> Downstream Propagation -> Receipt/Trace`.

### C. Behavioral / Craft Parity
Outputs need not be identical strings, but the live provider execution must preserve the architecture’s expected behavior and must not collapse in structure, causality, ensemble distribution, voice, subtext, physicalization, repetition/template resistance or other locked craft dimensions relative to the virtual/preformal evidence.

### D. Architecture x Renderer interaction
P07 must distinguish architecture contribution from renderer/model capability. A strong LLM result by itself is not evidence that Literary OS architecture caused the gain.

## 5. Correct role boundary
LLM owns creative cognition and literary realization:
Whole Series Plan -> Long Arc -> Episode Allocation -> Detailed Episode Synopsis -> Ensemble/Social Ecology -> Thread/Relationship -> Event/Plot-Axis Ownership -> THICK Sequence/Boundary -> Scene Plan/Contract -> Dialogue/Action/Subtext -> Final Surface Realization.

Python/code owns bridge/orchestration/state/retrieval/evidence/validation/risk-blocking/commit/rollback/lossless serialization.

Hard rule: `PYTHON_LITERARY_PROSE_GENERATION_BYTES = 0`.
Python may PASS/HOLD/REPLAN/RERENDER or route to the minimum responsible ancestor, but must not rewrite literary sentences.

## 6. Session-close delta recovery and physical authority normalization completed in the follow-up session
The follow-up session first recovered the previous session’s missing method/state delta into the 5-part / 8-package authority structure. Historical CP0 evidence was preserved, while the stale interpretation `CP0 passed, therefore API/Craft-Parity is next` was superseded.

The recovered current interpretation became:
`R-A historical re-audit complete -> R-B -> R-C -> R-D -> R-E -> R-F live parity -> R-G preformal freeze -> fresh sample -> revised R140 preregistration -> new G0 -> formal R140`.

The recovery work changed only current/control overlays and preserved the immutable historical/production/DB volumes as byte-unchanged where required.

## 7. R-B Narrative Architecture Closure — CLOSED in the follow-up session
R-B was implemented and independently rechecked as preformal engineering, not a formal experiment. The closure targets were:
1. Social Ecology Graph as an active runtime contract.
2. Event / Plot-Axis Ownership as a first-class runtime contract.
3. Detailed Episode Synopsis as a distinct LLM planning layer.
4. THICK Sequence + Boundary semantics as active parent-layer contracts.
5. Consumer Fidelity Gates from parent layer to child layer and into rendering/planning handoff.

A packaging defect was discovered during regression work: the CP0 Active Snapshot omitted `tools/` even though the canonical tool files still existed inside an earlier nested sealed candidate package. Five original tool files were recovered byte-for-byte from preserved authority rather than rewritten.

Historical tests were kept as history and not deleted. Current non-history regression scope then reproduced **128/128 PASS**, with R-B-specific tests **7/7 PASS**.

Do not reinterpret this as formal R140 evidence. It is preformal runtime/consumer closure.

## 8. R-C Decision Architecture Closure — CLOSED in the follow-up session
R-C closed the decision architecture around:
- LLM-owned Candidate Portfolio.
- LLM Plan Critic.
- R82-style non-compensatory safety floors.
- Primary/backup decision semantics.
- Referential integrity for `owner_axis_id` and `primary_group` against active R-B contracts.
- Actual downstream THICK-sequence input binding from the selected primary plan rather than receipt-only logging.
- Fail-closed safety semantics: only exact PASS can pass; undefined tokens become HOLD.
- Decision receipts binding raw safety results and allowed-context hashes.

The session reproduced **15/15 R-C-specific PASS** and **143/143 current non-history regression PASS**, and then integrated R-C into the current physical 8-package authority.

Latest physical authority produced before the later backend failure was reported as:
- CONTROL R31
- PART-A R30
- PART-B1 R10 byte-unchanged
- PART-B2 R31
- PART-C1 R10 byte-unchanged / ENG:R47 Production
- PART-C2 R30
- PART-D1 R10 byte-unchanged
- PART-D2 R10 byte-unchanged

A handoff audit was reported PASS at that R-C closure point. In a new session, revalidate SHA256/CRC/fresh extraction from the provided physical artifacts before treating file-level hashes not written here as newly verified.

## 9. R-D Long-Horizon Closure — ACTIVE / NOT CLOSED
R-D began after R-C and must not be reported as closed.

Preformal targets:
- Character / Relationship / Thread / Group / Membership / Group-Relation / Plot-Axis state carry.
- Authorized Novelty with real gap justification and atomic canonical registration.
- Narrative-state snapshot / exact rollback / resume, distinct from production-authority rollback.
- Controlled replan that cannot silently mutate Series Anchor or R-C Primary Plan.
- Synthetic EP06 -> EP07 -> EP08 continuation probe with exact parent-state consumption.

Problems already found during R-D preseal development:
1. Existing long-horizon compiler carried character/relationship/thread but not complete group/ecology state.
2. Existing rollback function targeted production authority, not narrative-generation state rollback.
3. Existing novelty path did not fully atomically register all required canonical atoms.
4. Canonical state hash domain risk: commit hash and next-episode parent hash could diverge if self-referential `last_state_hash` fields were not normalized consistently.

The hash-domain issue was identified before closure and was being repaired/tested with direct conditions such as:
- `EP06 commit == EP07 parent`
- `EP07 commit == EP08 parent`

At the last reliable local execution point before the backend failure, R-D-specific work had reached **17/17 PASS** and the current non-history regression scope had reached **160/160 PASS**, but a final referential-integrity / preseal audit was still pending. Therefore:

`R-D = ACTIVE / NOT CLOSED / DO NOT RESEAL AS CLOSED YET`.

The next local task after backend recovery is to finish dangling-reference mutation tests for relationships, memberships, threads and related canonical state; rerun the independent/fresh-extraction test scope; only then decide whether R-D may close.

## 10. Current infrastructure failure
At the end of the follow-up session the CAAS/container/python execution backend again returned `ClientError` for minimal commands including simple shell checks and Python execution.

Current infrastructure status:
`CONTAINER_EXECUTION_BACKEND_CLIENTERROR__MINIMAL_COMMANDS_FAIL__PYTHON_BACKEND_FAIL__ENGINE_FAILURE_NOT_ESTABLISHED`.

Rules while this persists:
- Do not claim new local runtime PASS/FAIL beyond the last reliably executed evidence above.
- Do not fabricate ZIPs, SHA256, CRC, fresh-extraction results or closure records.
- Do not advance R-D to CLOSED.
- Analysis/preregistration may continue, but executable closure and physical authority reseal must wait for a healthy runtime.

## 11. Exact next-session order
1. Read this R2 handoff and the historical R1 handoff.
2. Confirm container/python backend health with minimal commands.
3. Load and fresh-validate the latest physical R-C 5-part / 8-package artifacts supplied by the developer.
4. Reconfirm global invariants: formal count 137, R140 0/0/0, ENG:R47 immutable, DB59 frozen hash unchanged.
5. Reconfirm R-B and R-C current regression evidence from fresh extraction if runtime is healthy.
6. Materialize the Evidence/Parity Doctrine above into the top-level current authority if it is not already physically present in the provided R-C artifacts.
7. Resume R-D from ACTIVE state; complete referential-integrity mutation tests and preseal audit.
8. Only after R-D fresh validation PASS: make the R-D individual sealed package and integrate/reseal the 5-part / 8-package authority.
9. Continue R-E Surface Craft Closure.
10. Only after R-B through R-E are closed: perform R-F live Reference-vs-Actual Engine Craft Parity using the same locked provider/model/settings.
11. Then R-G preformal freeze -> fresh deterministic sample -> revised R140 preregistration -> new G0 -> formal R140.

## 12. Scientific claim boundary
The current project status is:

`THREE_STAGE_RESEARCH_LINEAGE_REAUDITED`
`__SESSION_CLOSE_DELTA_RECOVERED`
`__R_B_CLOSED`
`__R_C_CLOSED_AND_PHYSICALLY_INTEGRATED`
`__R_D_ACTIVE_NOT_CLOSED`
`__CONCEPT_VIRTUAL_LIVE_FORMAL_EVIDENCE_LAYERS_SEPARATED`
`__PARITY_DOCTRINE_REINFORCED`
`__P07_PREFORMAL_PROBLEM_DISCOVERY_AND_REPAIR_ACTIVE`
`__FORMAL_R140_ATTEMPT_OUTPUT_SCORE_0_0_0`
`__CONTAINER_BACKEND_CLIENTERROR_AT_SESSION_TRANSITION`.

Do not call P07 complete, do not call R-D complete, and do not promote the PRE-P07/PRE-R140 candidate to Production on the basis of current preformal evidence.