# P07-I4C 50-Scene Interface-Driven Whole-Episode Rerender — Preregistration R1

Date: 2026-09-08
Classification: DEVELOPMENT / PREFORMAL / NO FORMAL COUNT DELTA
Parent authority: `CURRENT_PHYSICAL_AUTHORITY__P07_I4B_LITERARY_SURFACE_CONTRACT_R1`
Parent Package Set SHA256: `3103c0448649a9d89294abc3e5ba3fde2bf9ac415ea5e8d69d530562e4fdcb2b`
Parent reconstructed C2 SHA256: `fd5e3d78b3e127a6c2a252d5a825b8355e88438a18cc386239e6ce006e91ebaa`
Frozen DB59 SHA256: `a5cff0fcd43584220f41a4be85b112c7fc5246977d856797d2676546bccb6bc9`
Formal scored count before probe: 137
R140 attempts/outputs/scores before probe: 0/0/0
Real OpenAI Live evidence eligibility: FALSE unless a trusted provider receipt exists.

## 1. Purpose
P07-I4B demonstrated causal adoption and an internal human-competitive development signal for `LiterarySurfaceContractR1` on eight frozen scenes. P07-I4C tests whether the same interface generalizes to an entire 50-scene broadcast episode without losing continuity, ensemble ecology, thread/payoff state, sequence function, or state-carry integrity.

This is not a prompt-tuning experiment. It is a whole-episode interface-generalization and closed-loop development probe.

## 2. Hypothesis
Applying `LiterarySurfaceContractR1` to all 50 frozen P07-I3 scene plans will produce a 35,000-45,000 Unicode-character Korean broadcast screenplay that is materially more literary and less procedurally expositional than the sealed P07-I3 control while preserving episode/sequence semantics, ensemble ownership, thread state, and terminal carry. If a real defect appears, the responsible-ancestor router should repair only the minimum responsible layer and re-lower affected descendants before final commit.

## 3. Frozen control / target
CONTROL: exact sealed P07-I3 final 50-scene screenplay and its repaired architecture/state carry.
TREATMENT: same 50-scene frozen episode architecture under current P07-I4B active engine with `LiterarySurfaceContractR1` inserted between `SCENE_PLAN` and `SURFACE_REALIZATION`.

Frozen episode identity: synthetic original work `마감 전 30분`, EP02.
Scene count: exactly 50.
Sequence architecture: existing repaired P07-I3 10-sequence architecture.

Upstream invariants unless a preregistered diagnostic triggers responsible-ancestor repair:
- episode trajectory;
- sequence ownership;
- scene order;
- scene function;
- principal cast set;
- required information movement;
- relationship movement;
- active thread/payoff commitments;
- exit state;
- no future/source leakage;
- no new unsupported principal event/world fact.

## 4. Generation boundary
All 50 scene contracts and treatment prose may be authored by the in-session foundation LLM for development-only evidence and must be labeled `IN_SESSION_LLM_SURROGATE__NONLIVE`.
Python/runtime may only validate, route, hash, measure, audit, and commit state; Python literary prose generation = 0.
Human-anchor DB59 text must not be inspected while treatment prose is being authored. Human anchors may be selected only after the treatment screenplay and all repair cycles are sealed.

## 5. LiterarySurfaceContractR1 whole-episode requirement
Every scene must carry and validate:
1. semantic invariants;
2. character play state;
3. dialogue information budget;
4. relationship surface map;
5. voice state;
6. subtext/physicalization;
7. rhythm contract;
8. surface firewall.

Provider/renderer literary payload must contain the consumed contract. Non-consumed metadata must not affect literary payload hash.

## 6. First-pass broadcast gates
First-pass whole episode must satisfy or produce a diagnostic for repair:
- 50 scenes exactly;
- 35,000-45,000 Unicode characters preferred final target;
- unauthorized speakers = 0;
- critical semantic/continuity violations = 0;
- source/future leakage = 0;
- exact long-dialogue duplicate ratio <=0.05;
- exact long-narrative duplicate ratio <=0.05;
- no forbidden engine/meta terminology in surface;
- procedural/control vocabulary density <=12 per 1,000 Unicode characters across the full episode;
- mean non-empty line length <=45 Unicode chars OR >=20% reduction vs P07-I3 control;
- protagonist scene share <=0.65;
- protagonist-group sequence ownership <=0.60;
- non-protagonist-owned sequences >=4;
- non-protagonist owner groups >=2.

## 7. Craft-development diagnostics
Internal craft evaluation prioritizes:
- dialogue naturalness;
- character voice differentiation;
- subtext/implication;
- physicalization/action specificity;
- relationship/power specificity;
- exposition discipline;
- pacing/line economy;
- scene turn/exit pressure;
- emotional progression;
- ensemble/social ecology causality;
- thread/payoff continuity;
- episode/sequence functional fit.

No simple average may compensate for a critical continuity or unsupported-invention failure.

## 8. Responsible-Ancestor Repair rule
If a material defect appears, repair must route to the minimum responsible layer:
- pure prose/voice/exposition defect -> `LITERARY_SURFACE_CONTRACT` or `SURFACE_REALIZATION`;
- scene function/turn defect -> `SCENE_PLAN`;
- multi-scene causal/order defect -> `SEQUENCE_PLAN`;
- ensemble ownership/ecology defect -> `ENSEMBLE_ECOLOGY_PLAN`;
- episode trajectory/thread allocation defect -> `EPISODE_PLAN` or responsible upper layer.

Maximum repair cycles: 2.
Affected descendants must be re-lowered/re-rendered; protected unaffected siblings must remain plan-level stable where possible.
If defect remains after two cycles: `HOLD_REDESIGN_REQUIRED` rather than weakening any threshold.

## 9. State Commit/Carry gates
Final acceptance requires:
- final broadcast gates PASS;
- diagnostic ledger closed;
- no unresolved critical semantic/continuity/thread defect;
- canonical state commit succeeds;
- carry hash generated for next episode;
- commit is prohibited if final plan/surface status is HOLD/ERROR.

## 10. Whole-episode Control comparison
After final treatment sealing, create 12 stratified anonymous pairs sampled from the 50-scene episode across:
- workplace/procedural;
- family/relationship;
- ensemble/community;
- investigative reveal;
- conflict/power;
- quiet emotional;
- midpoint pressure;
- convergence/exit.

Stage A whole-episode treatment vs exact P07-I3 control PASS requires:
- Treatment wins >=9/12;
- Treatment losses <=3/12;
- zero critical semantic/continuity violation.

## 11. Fresh-human blind development benchmark
Only after final treatment sealing and Stage A PASS, select 12 fresh DB59 human broadcast-script anchors by matching scene function, masking source/title/names and randomizing left/right after candidate sealing.
Do not knowingly reuse the exact I4B Stage B passages where feasible.

Human-competitive whole-episode development signal requires BOTH:
- Candidate wins >=3/12;
- Candidate wins + ties >=6/12;
- zero critical semantic/continuity violation.

If failed: `HOLD__WHOLE_EPISODE_SURFACE_NOT_HUMAN_COMPETITIVE__NO_LIVE_PROMOTION`.

## 12. Regression / adoption rule
Any new code required for whole-episode contract construction, diagnostics, routing, or state integration must pass:
- focused tests;
- exact final packaged-C2 fresh-materialization nonhistorical regression with pytest exit code 0.

No treatment prose may be hardcoded into active engine policy.

## 13. Physical promotion rule
Only if mechanical, semantic, whole-episode Stage A, human Stage B, responsible-ancestor, state-carry, and regression gates all pass may I4C be physically promoted.
Promotion requires changed bytes to be resealed into canonical 5 Parts / 9 Packages; unchanged packages remain byte-identical; CRC/duplicate/unsafe/nested/secret/cross-package reconstruction/Manifest/Trust Root all PASS.

## 14. Non-claims
P07-I4C development PASS would not establish real OpenAI Live parity, external multi-human consensus, human-writer equivalence, Production promotion, RFV3, CP1 Live, official R-F/R-G, or Formal R140 qualification.
