# P07-I1 Closed Narrative Operating Loop Integration Pretest — Preregistration R1
Date: 2026-09-07
Classification: PREFORMAL DEVELOPMENT EXPERIMENT / NO FORMAL COUNT DELTA

## Purpose
Integrate the currently separated hierarchical semantic-planning path and the local portfolio/selector/repair/state-commit path into one canonical orchestration call and evidence chain, without claiming full Narrative Knowledge Bus or Ensemble Ecology completion.

## Frozen starting authority
- Current physical authority: CURRENT_PHYSICAL_AUTHORITY__P07A_RFV2_CONTROLLED_RECOVERY_R1
- Current physical C2 reconstructed SHA256: 1a9355169650d66af0a3f44fb867bad1c00e5dc643e8f28443d1b2f6c6cde62d
- Frozen DB59 SHA256: a5cff0fcd43584220f41a4be85b112c7fc5246977d856797d2676546bccb6bc9
- Formal scored count remains 137; R140 remains 0 attempts / 0 outputs / 0 scores.

## Pre-result engineering repair admitted before integration test
The previously discovered retrieve_many() vs retrieve() donor-selection drift is repaired only by making retrieve_many() delegate to the runtime-canonical retrieve() implementation. No retrieval threshold, query field, TF-IDF setting, top-k, database member, or semantic profile field may be changed.

## Hypothesis
H1: One orchestrator can run hierarchical semantic planning with the current DB59 retrieval path, bind a portfolio/selector decision to the resulting episode artifact, route a diagnostic to the existing repair router, and commit/carry the resulting narrative state without bypassing existing semantic-contract, evidence, or fail-close guards.

## Research questions
1. Can the two previously separate mechanics paths execute as one ordered transaction?
2. Does the integrated result preserve the exact hierarchical planning evidence and DB59 retrieval receipt?
3. Does the selector produce a COMMIT from explicitly grounded candidates and is the selected candidate hash bound into the integration receipt?
4. Can an injected structural diagnostic route to the minimum responsible ancestor using the existing repair router?
5. Can the accepted episode artifact be passed into continuous-state integrity and produce a committed carry hash?
6. Do all pre-existing nonhistorical regression tests remain PASS after the minimal integration change?

## Input / provider boundary
- Use only current authority bytes and sealed development fixtures already present in the current C2 overlay.
- Use Scripted/Test-Double provider for this integration mechanics pretest.
- No live-provider claim, no literary-quality claim, no formal experiment count delta.
- Python may orchestrate, validate, hash, and route but must not author literary prose.

## Implementation boundary
Allowed changes:
1. retrieve_many() parity repair by delegation to retrieve().
2. New integrated orchestration module/tool/tests that compose existing modules.
3. Integration receipts/evidence files and package authority documentation.

Disallowed changes:
- retrieval thresholds or vectorizer settings;
- DB59 content;
- existing semantic stage schemas;
- selector scoring thresholds unless already parameterized by the caller;
- renderer literary surface generation;
- R140/RFV3/CP1 status.

## Frozen integrated path
HIERARCHICAL_SEMANTIC_PLANNING
-> DB59_RETRIEVAL
-> SEMANTIC_CONTRACT_VALIDATION
-> PORTFOLIO_CONSTRUCTION_FROM_ACCEPTED_EPISODE_ARTIFACT
-> SELECTOR_COMMIT
-> DIAGNOSTIC_REPAIR_ROUTING
-> CONTINUOUS_STATE_INTEGRITY
-> CANONICAL_STATE_COMMIT
-> STATE_HASH_CARRY

This R1 does not yet insert ENSEMBLE_ECOLOGY_PLAN into the semantic main path; that is the next integration experiment after I1 if I1 passes.

## Pass gates
G1. retrieve_many parity: for all sealed six development fixtures, retrieve_many() and direct retrieve() return identical decision, selected_record_ids, literary_payload_sha256, confidence band, and top-work ordering.
G2. Integrated orchestrator returns status PASS on the frozen development integration fixture.
G3. Hierarchical semantic planning sub-result status PASS and all semantic-contract verdicts ACCEPT.
G4. DB59 retrieval sub-result is not bypassed; database/retrieval receipt hashes are present and bound into the integrated evidence root.
G5. Selector action COMMIT and selected candidate hash/id are present in the integration receipt.
G6. Diagnostic repair route returns REPLAN_PARENT (or the exact existing minimum-responsible-ancestor action expected by the frozen injected diagnostic) and target stage is bound.
G7. Continuous-state integrity status PASS and carry.committed == true with non-empty new_hash.
G8. Existing full nonhistorical regression passes with no prior test removed or weakened; new tests may increase the total count.
G9. Secret-pattern hits 0; Live-provider evidence claim false; formal-count delta 0; R140-attempt delta 0.

## Failure policy
Any failed gate is preserved as FAIL/HOLD. No threshold retuning, donor retuning, fixture replacement, or post-result weakening of gates is allowed.

## Post-pass physical closure
If I1 passes and code/evidence changes are adopted, propagate them into the canonical 5 Parts / 9 Packages. Changed packages receive new SHA256; unchanged packages require byte-identical proof. Rebuild Manifest and Trust Root and only then declare the new current physical authority.
