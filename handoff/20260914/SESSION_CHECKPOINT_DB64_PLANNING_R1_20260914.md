# SESSION CHECKPOINT — DB64 → SHOWRUNNER PLANNING R1

Date: 2026-09-14
Physical parent: SYNC-R34

## Purpose

This document preserves the research path of the long session so a new session can continue without reconstructing hidden intermediate reasoning or repeating completed experiments.

## Stable authorities

- Physical Research Authority: SYNC-R34
- Active Engine: P07-I4H Recovery R3
- Production: ENG:R47
- Formal scored: 137
- Latest Formal: R138
- Formal R140: 0/0/0
- DB59 frozen historical benchmark SHA256: `a5cff0fcd43584220f41a4be85b112c7fc5246977d856797d2676546bccb6bc9`
- DB64 current development/semantic candidate SHA256: `19f3c446a73408045d02d4d99e168251dca42da3bfa00abaff1d8f9159d7ea46`

## Scientific question

Can DB64's improved semantic knowledge be safely consumed by Literary OS and cause better planning while preserving the strong historical DB59 baseline?

The investigation decomposed this into four layers:

1. Data/schema compatibility
2. Retrieval quality
3. Structured donor/directive interface
4. Full Showrunner planning effect

## Data and compatibility findings

The session repaired and qualified:

- heterogeneous DB64 thread-schema reading
- source-row preservation
- semantic-node deduplication without provenance deletion
- canonical endpoint priority
- non-overwriting DB59 fallback
- durable Plant→Continue/Escalation→Payoff lineage handling
- identity-safe structured functional abstraction

Important boundary: the qualified research fuel is not raw DB64 alone. It is DB64 current native semantic view plus provenance-bound, non-overwriting DB59 legacy compatibility where required.

## Experiment genealogy retained as authority

### A2R8
Aborted / not scored after runtime remount caused loss of secret mapping bytes. Mapping was never guessed or recreated.

### A2R9 — Clean Thread Lineage Resolver Reconstruction
PASS.

Qualified structural lineage resolver while preserving DB64 native source aliases and non-overwriting fallback semantics.

### A2R10 — Clean Rolling-Fuel Retrieval Qualification
PASS.

Observed:
- 22 Treatment wins
- 18 ties
- 8 Control wins
- 40/48 Treatment nonloss
- decisive pairs: 30
- decisive Treatment win rate: 73.33%
- Plant/Payoff: 6W/0T/2L
- additive-native exposure: 42/48
- additive-exposed nonloss: 80.95%

Qualified claim:

`DB64 current native view + A2R9 provenance-bound non-overwriting DB59 fallback` is qualified as rolling research retrieval fuel.

Not established:
- raw DB64-alone superiority
- planning-level effect
- Active Engine promotion
- Production promotion
- Formal promotion

### A2R11
ABORTED_NOT_SCORED after secret mapping / donor input evidence was lost during runtime remount while plan bytes survived. Evidence-custody failure, not a scientific PASS/FAIL.

### A2R12
FAIL. Retrieval differences were compressed by boolean planning thresholds; 11/12 pairs became effectively identical.

### A2R13
FAIL. Continuous feature transmission increased distinct plans but cross-axis tradeoffs prevented a stable plan-level advantage.

### A2R14
FAIL_PREBLIND. Target-axis isolation worked, but 0–4 quantization again collapsed signal.

### A2R15
FAIL_PREBLIND. Semantic directive approach improved representation but random samples often retrieved the same underlying donor functions in both arms.

### A2R16
FAIL, 6W/2T/4L. Additive-native triggered sampling produced enough differences, but directive extraction still selected weak or misleading functions.

### A2R17
FAIL_PREBLIND. Real identity leakage discovered: short character aliases and work titles remained in directive prose. No blind mapping was created.

### A2R18
PASS. Identity-safe anonymization boundary qualified mechanically, but later work showed that raw donor prose itself remained an unsuitable planning boundary.

### A2R19–A2R22
These experiments progressively showed that ranking and reranking alone could not solve the planning problem. Long donor prose and tokenized identity placeholders still diluted functional planning signal.

### A2R23
FAIL, 5W/4T/3L.

Structured Functional Abstraction removed raw donor prose and identity leakage. Direction improved substantially over A2R21/A2R22, but full qualification was not reached.

### A2R24
FAIL_PREBLIND. Case-conditioned structured utility produced only 8/12 meaningfully distinct pairs.

### A2R25
Canonical monotonic additive selector: FAIL_PREBLIND. Protecting every baseline function too strictly produced too few useful changes.

### A2R26 — Canonical Supplemental Advisory + Abstention
PASS, 10W/2T/0L.

This is a major retained finding:

> Protect DB59 baseline functions; offer DB64-native improvements only as optional advisories; allow abstention when no safe added value exists.

This is the current best interface doctrine.

### Duplicate branch reconciliation
Runtime interruption produced duplicate A2R25/A2R26 numbering. Authority rule was frozen:

> A physically complete preregistration→output→judgment→result→closure chain outranks a branch whose parent preregistration bytes cannot be recovered.

Duplicate side branches are diagnostic only.

### A2R28
FAIL, 1W/10T/1L.

Advisory interface was safe, but the planner reduced advisory novelty to labels/spans and did not alter event architecture in most cases.

### A2R29
FAIL, 3W/7T/2L.

Function-to-sequence allocation increased sensitivity, but different target axes created tradeoffs and duplicate sequence-function injection.

### A2R30
FAIL_PREBLIND.

Single-advisory arbitration removed cross-axis conflict and duplicate injection, but realization collapsed rich advisory signatures into one preferred token; only 5/12 plans were distinct.

### A2R31 — Full Novelty Signature Realization
FAIL, 6W/2T/4L.

Important improvements:
- 12/12 selected Control/Treatment plans were materially distinct
- identity leakage 0
- duplicate injection 0
- one advisory at most per plan
- full novelty signature reached sequence realization

However frozen quality gate failed:
- wins required >=7; observed 6
- nonloss required >=10; observed 8
- losses required <=2; observed 4

Interpretation:

The remaining problem is no longer signal transmission. The signal now reaches planning. The remaining problem is **utility control over the transmitted novelty**: too much, case-irrelevant, or poorly placed novelty can still make a Treatment plan worse.

## Current bottleneck

Current final bottleneck:

`Structured DB64 novelty → utility arbitration → sequence placement → event architecture`

The retrieval system is qualified. The optional advisory/abstention doctrine is qualified. Full planning effect is not yet qualified.

## Exact resume point

1. Treat A2R31 FAIL as immutable.
2. Diagnose the four Treatment-loss cases only.
3. Do not lower `7W / 10 nonloss / <=2L`.
4. Do not rerun A2R31 outputs.
5. Preregister a fresh successor using unseen cases.
6. Preserve:
   - DB59 protected baseline
   - DB64 optional additive advisory
   - abstention
   - one advisory maximum unless a later preregistration explicitly changes it
   - identity-free structured abstraction
   - full novelty signature
   - immutable plans before mapping
   - balanced fresh mapping
   - blind judgment seal before unblind
7. Repair only the localized planning-utility defect.

## Session-safety operating rule

From this point onward, every new experiment must be checkpoint-safe:

`PREREG → INPUT SEAL → OUTPUT SEAL → RECOVERY BUNDLE → BLIND/MAPPING SEAL → JUDGMENT SEAL → UNBLIND → IMMUTABLE CLOSURE → HUB POINTER UPDATE`

If a session may terminate before the final steps, stop at the last sealed boundary rather than continuing with an unpersisted state.
