# 깊이 기준 검증 — 열화 대조 2AFC 결과

문서 ID: LOS-DEPTH-VALID-RESULT-V1.0
집행일: 2026-08-10 · 사전등록서: LOS-DEPTH-VALID-PREREG-V1.0 (sha256 `9752453868e2c068…`, 렌더 0건 시점 봉인)

## 1. 판정

**PASS. 깊이 기준 v0.1은 유효하다.**

ARM H(원본 후판) 승 **22 / 24**, 이항 단측 **p = 0.00002**. 사전 확정선은 H ≥ 17/24 였다.

판정자별 H 선택: J1 22/24 · J2 22/24 · J3 21/24. 72표 중 71표가 3인 일치.

## 2. 무엇을 조작했고 무엇을 고정했나

같은 작품(경성스캔들), 같은 회차, 같은 씬, 같은 시퀀스 구조. 바꾼 것은 후판 내용 텍스트 6종뿐이며, 열화 규칙은 내가 지어낸 것이 아니라 **101번째프로포즈(실제 미달작)에서 자동 추출한 정형문 골격**을 슬롯 충전한 것이다. 슬롯에 넣은 값은 **이미 박판 씬카드에 있던 씬 제목**뿐이다.

| 팔 | 다양도@813 | 정형문% | 상투피복% |
|---|---|---|---|
| ARM H 원본 | 0.799 | 0.0 | 0.0 |
| ARM D 열화 | 0.272 | 73.4 | 77.6 |
| 참조: 101번째프로포즈 실제 | 0.248 | 96.4 | 88.8 |

ARM D는 정본에 실제로 들어 있는 미달작보다 **덜 열화**되어 있다. 즉 이 효과는 이미 DB 안에 있는 수준의 공허함에서 발생한다.

## 3. 교락 점검 (사후, 사전등록 외)

- **길이 교락**: ARM H 렌더가 더 긴 경우가 24문항 중 19건(중앙 1,059자 vs 874자). 길이로만 예측하면 19/24 — 실제 22/24 와 비슷하다. 따라서 **부분집합 검정**을 했다. **ARM D가 더 길었던 5문항에서 H가 5전 5승**(단독 p = 0.031). 길이가 결과를 만든 것이 아니다.
- **판정 사유 감사**: 24×3 사유를 전수 읽었다. 전부 원문의 구체 사건·대사·정보변화를 지목한다(예: I03 "수현이 총독부 보안과임을 밝히고 완이 굳는 결정적 정보변화 재현", I24 "체포인 줄 알았던 두 사람이 시신으로 확인되는 정보 전복"). 사전등록 §8-2 무효 조건(문체·형식 사유로 몰표)에 **해당하지 않는다**.
- **A/B 위치 편향**: A 선택 31 / 72. 편향 없음.
- **H가 진 2문항**: I18은 H 렌더가 장면 주체를 다른 인물로 옮겼고, I19는 엔딩 순서를 뒤집었다. 둘 다 D의 성공이 아니라 **H의 집필 실패**다.

## 4. 이 결과가 말하는 것과 말하지 않는 것

말하는 것: 후판의 깊이는 씬 생성 결과를 실제로 좌우한다. 정형문으로 채운 설계도는 같은 씬을 원문에서 멀어지게 만든다. **깊이 지표는 문체 취향이 아니라 성능 지표다.**

말하지 않는 것 (사전등록 §9 그대로):
1. 경성스캔들 1작에서만 본 것이다. 일반화 근거 없음.
2. 열화는 기계적이다. 사람·GPT가 얕게 쓴 후판이 같은 방식으로 얕은지는 별개다.
3. **합격선 0.748이 옳다는 증명은 아니다.** 검증된 것은 축의 방향이지 임계값이 아니다. 임계값은 여전히 CT-07R T팔 앵커 유래의 잠정치다.
4. 2AFC는 상대 비교다. 두 팔 다 원문에서 멀 수 있다.
5. CT 번호를 부여하지 않는다. CT-08A-R 미판정 상태에서 확증 트랙을 늘리지 않는다.

## 5. 사전등록에 따라 발효되는 조치

PASS 이므로 DEPTH_STD §5 세 조치를 집행 대상으로 승격한다.

1. **101번째프로포즈 후판 격리** (184시퀀스·15회차). 다양도 0.234·정형문 99.1%. 내가 오늘 편입 게이트를 통과시킨 물건이며, 게이트에 깊이 항이 없었던 것이 원인이다.
2. **CT-09 앵커 교체**: 너의목소리가들려(0.445, 미달) → 내여자친구는구미호(0.755, 합격).
3. **편입 게이트에 충실도 항 추가**: 회차 단위 다양도 하한. 구조·정합·원문근거만 보던 현행 게이트는 공허한 후판을 막지 못한다는 것이 실증되었다.

산출물: `C:\claude\depth_valid\` (degrade2.py·mktmpl.py·mkitems.py·mkprompts.py·measure_arms.py, prompts/ 48, render_inputs/ 48, renders/ 48, judge/ 24문항+3판정표, BLIND_MAP.json, PREREG·RESULT)
