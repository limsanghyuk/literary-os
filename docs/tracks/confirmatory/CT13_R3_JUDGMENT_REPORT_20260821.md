# CT-13 R3 판정 보고 — **FAIL (사전등록 규칙)**, 단 계획층 효과는 전 지표 유의 (2026-08-21)

**문서 ID**: LOS-CT13R3-JUDGMENT-V1.0 · 킷: CT13_R3_EXECUTION_KIT_V2(SHA 65544694…) · 렌더 봉인 manifest `b9a7c420dde4c58d…` · 정답키는 렌더 봉인 **이후** 작성(타이밍 규칙 충족, 키 내부에 봉인 sha 기록).
**★인증 한계(가장 먼저 공시)**: 렌더러 6세션·커스터디언 1세션·채점자 3세션을 모두 **동일 호스트 세션(Claude)**이 격리 배치했다. 킷 §7은 단일 세션의 PASS 인증을 금한다. 따라서 본 문서는 **계산된 결과(computed result)**이며 **독립 인증(certified PASS)이 아니다.** 판정 수치는 봉인 해시가 일치하는 한 제3자가 그대로 재현·검증할 수 있다.

## 1. 집행 경로 (킷 순서 준수)
① 입력 48 검증 PASS → ② 격리 렌더러 6인 × 8건 = **48/48**, 스키마 위반 0 → ③ `freeze_renderer_outputs.py` **SEALED** → ④ **별도 커스터디언**이 정답키 12앵커 작성(렌더 산출물 미열람, P1/P2/P3 평가가능면 12/12) → ⑤ `build_blind_scorer_packets.py` → 48케이스 × 3부(팔 라벨·언블라인드 맵 비공개) → ⑥ **격리 채점자 3인** 각 48건 → ⑦ `analyze_blind_scores.py`.

## 2. 판정
> ## **verdict: FAIL** — P4(미래정보 누출) 합의 위반 **1건**으로 사전등록 §6-1("48건 전체에서 P4 위반 = 0") 불충족.

| PASS 조건 | 요구 | 실측 | 충족 |
|---|---|---|---|
| ① P4 위반 = 0 | 0 | **1** | **✗** |
| ② C>B, P1/P2/P3 중 2개 p<.05 | 2개 | **3개 전부**(p=.0039 / .00098 / .00049) | ✓ |
| ③ C>N, P1 또는 P2 p<.05 | 1개 | **P1 .0020 · P2 .0020**(+P3 .00049) | ✓ |
| ④ 채점자 일치 C-B·C-N ≥0.80 | 0.80 | **C-B 0.889 · C-N 0.963**(쌍별 평균) | ✓ |
- 부수: B-A 일치 0.796(보조 지표, 판정 미사용).

## 3. 팔별 결과 (3인 다수결, n=12)
| 팔 | P1 유예정책 | P2 부채일관성 | P3 상태목표(0~2) | P4 위반 |
|---|---|---|---|---|
| A 과거상태만 | 0.25 | 0.17 | 0.17 | 0 |
| B +회차아크 | 0.25 | 0.08 | 0.25 | 0 |
| **C +회차계획** | **1.00** | **1.00** | **2.00** | 0 |
| N 불일치계획 | 0.17 | 0.17 | 0.08 | **1** |
**C는 12앵커 전부에서 세 지표 만점**이다. A·B는 0.1~0.3 수준에 머문다. C−B와 C−N 모두 부호검정에서 **패배 0건**(9~12승 0패)이다.

## 4. 실패한 1건의 정체 (진단)
- **R3A09_N** — 불일치 계획을 받은 네거티브 팔. 3인 **만장일치**로 P4 위반 판정. 사유 일치: *보호된 유예축(민효린 재투입 금지)을 넘어 해당 인물을 능동 행위자로 재투입*, 출구상태도 미달.
- 즉 **누출은 C(계획층)가 아니라 N(대조군)에서 발생**했다. 계획층이 미래 정보를 새게 한 것이 아니라, **불일치 계획을 렌더할 때 렌더러가 보호 정책을 어긴 것**이다.
- 사전등록은 팔 구분 없이 "48건 전체 P4=0"을 요구하므로 규칙대로 FAIL이다. **결과를 본 뒤 이 조항을 완화하지 않는다.**

## 5. 해석 (과장 금지)
1. **계획층(EpisodeSynopsisPlan)의 증분 효용 신호는 이례적으로 강하다.** 세 주지표 전부, 두 대비(vs B, vs N) 전부에서 유의하고 패배가 0이다. 다만 **사전등록 판정은 FAIL**이므로 이 신호는 "강한 관측"이지 "확증"이 아니다.
2. **N팔 처리 편차(렌더 보고서에 기공시)**: 렌더러들이 불일치 계획의 "구조만" 현재 작품에 이식했다. 이는 C에 **보수적**(불리한) 방향이었는데도 C−N이 큰 차로 유의했다 — 효과가 이 편차로 설명되지 않음을 시사한다.
3. **채점자·렌더러 동일 계열(claude)** 교란은 남는다. 특히 P3(상태목표 달성)가 C에서 만점 2.00인 것은 채점자가 계획 문구와 시놉시스의 **어휘 중첩**에 반응했을 가능성을 배제하지 못한다. → 재시험 시 **다른 계열 채점자** 필수.

## 6. 승격 판단
- 사전등록 §8: **R3 PASS만이 34작 대량 EpisodeSynopsisPlan 저작을 승인**한다. 본 결과는 FAIL이므로 **대량 저작은 여전히 미승인**이다.
- 권고: **R4 재시험**을 (a)N팔 P4 조항의 팔별 분리 여부를 **사전에** 재정의하고(예: "C팔 P4=0"을 주조건, N팔 위반은 대조군 진단으로 분리), (b)채점자를 비-claude 계열로 두고, (c)렌더러/채점자 호스트를 물리적으로 분리해 집행한다. 본 런의 48 렌더 산출물은 **봉인된 채 재사용 가능**하다(재렌더 불요, 채점만 교체하면 됨).

## 7. 산출물
`renderer_outputs/`(48+매니페스트·봉인) · `custodian/ANSWER_KEY.json` · `scoring_packets/`(패킷 3·결과 3·UNBLIND_MAP) · `CT13_R3_BLIND_EVALUATION_RESULT.json`. 전량 결정론 재현 가능.
