# 2026-06-21 SP-E.9 라운드#2 — P3 craft (show-don't-tell) 진짜 기교 학습 실측

라운드#1(P1 셔플=쉬운 신호) 후속. **P3 안티-LLM craft 쌍**으로 4070 1라운드 → base가 찍기 수준인 어려운 신호에서 per-token dW 대폭 양수.

## 데이터 (P3, 개발자 build() 게이팅)
- chosen=show-don't-tell(감각·행동·정적, 감정어 금지) / rejected=tell(감정 직접서술·설명조). 둘 다 gpt-4o-mini 생성 → **AI간(암기위험0)**.
- 토큰정확트림으로 길이매칭(tokΔ=0) 후 length_match 게이트 100% 통과. 중복0. work-level split **train 270 / held 250(≥250)·누설0**. 본문 verbatim 비기록.

## 결과 (held 250, epochs=1, ~3분 학습 + 측정)
| 지표 | W0 | W1 | Δ | (P1 라운드#1 대조) |
|---|---|---|---|---|
| per-token 승률 | **0.480** | **0.884** | **dW_pt +0.404** | (P1: 0.952→1.0, +0.048) |
| sum 승률 | 0.144 | 0.584 | +0.440 | |
| per-token 마진 | −0.032 | +0.248 | +0.280 | |
| KL/token | — | **0.127** | τ=0.50 | (P1: 0.406 과적합) |

**G_LOOPC_WINRATE(per-token): [PASS] dW_pt>0 · [PASS] KL≤τ · [N/A] c3 → ADOPT-candidate.**

## 해석 — 첫 진짜 craft 학습 증거
- **base W0=0.480(찍기), 마진 −0.032**: base 모델은 show/tell을 구분 못 함(P1과 정반대, 출발점이 우연). → 배울 게 실재.
- **W1=0.884, dW_pt+0.404**: 미학습 250쌍에서 craft 선호 학습·일반화. **길이중립·암기0·held 3중 통제** 하의 결과 = 메커니즘이 아니라 *기교*를 학습.
- **KL 0.127**: epochs 2→1로 P1 과적합(0.406) 해소. 건전.
- 단계 정합: Round#2(길이착시 ROLLBACK)→P1(메커니즘 증명)→**P3(craft 증명)**.

## 정직한 한계
1. rejected=gpt-4o-mini 의도적 평이체 → "유능 AI vs 평이 AI" craft축. 인간 명작 대비는 최종시험 보류.
2. W1=0.884 → 11.6% 잔여. 2. 선호 랭킹 향상이지 생성품질 직접 증명은 생성+critic 루프 필요.
3. 졸업(SP-E.10)=누적 adopt 라운드 필요(본건은 P3 단일 검증).

## 다음
- 순서 3: **c3 구조게이트**(생성 씬 비퇴락 측정) 켜기.
- 생산 혼합비(15/55/20/10)로 통합 라운드 → 누적 → Phase E Exit.
- 전부 4070 단독(A100/H100·RunPod 불요 확정).

## c3 R_path 측정 (순서 3 완료)
4070에서 P3 어댑터 off/on으로 씬 생성·코퍼스밴드 병리 비교.
- N=24: 1→2 (노이즈로 FAIL 표시) → N=48 재측정: **병리 5/48 → 2/48 감소**. energy 위반 1→0, dialogue 2→0. 피처평균 안정(conflict 0.43→0.35, energy 1.27→1.26).
- **c3_path PASS** (tol=2). craft 학습이 생성 병리 미증가(보상해킹 없음).

## ★P3 라운드 종합: 첫 완전 loop-C ADOPT
c1(per-token dW+0.404) ∧ c2(KL0.127) ∧ c3_path(병리 5→2) **세 조건 통과 = ADOPT**. 길이중립·암기0·held·분포안전 4중 통제. 전부 4070 단독(A100/H100·RunPod 불요).
범위: 완전 c3의 R_struct/R_pair(계획타깃)는 통합 생성본체 몫. R_path(보상해킹 안전)는 자체 통과.

## 순서 4 (다음): 졸업 누적
생산 혼합비(P1 15%/P3 55%/P2 20%/P4 10%)로 통합 라운드 → adopt 5연속 누적 → SP-E.10 Phase E Exit. P2(온폴리시)·P4(동점) 생성기는 개발자 스텁(미구현). 4070 단독 경로.
