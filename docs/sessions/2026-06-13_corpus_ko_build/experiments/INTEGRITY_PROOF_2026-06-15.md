# corpus_ko 무결성 정식 입증 — 인코딩 픽스 후 전면 재처리 (2026-06-15)

**계기**: NER LLM 폴백이 깨진 이름(ȯⱸ, ħϰ)을 반환 → 원천 추적 결과 **112편(코퍼스 25%)의 소스 txt가 UTF-16인데 utf-8로 읽혀 모지바케**. `readbest()`(utf-16→cp949→utf-8 폴백 + 한글비율 검증)로 112편 재변환 후 **전 파이프라인(parse→embed→ChromaDB→features→NKG) 재처리**. 본 문서는 그 결과의 무결성을 6개 불변식으로 입증한다.

## 입증 결과 (6/6 PASS)

| # | 불변식 | 측정 | 판정 |
|---|---|---|---|
| 1 | 텍스트 0-깨짐 | txt 456편, 한글비율<10% = **0편** | ✅ |
| 2 | 청크 수 | chunks/*.jsonl 455작품 = **62,514 청크** | ✅ |
| 3 | 임베딩 수 = 청크 수 | emb_cache 313샤드 = **62,514 벡터** | ✅ (=#2) |
| 4 | ChromaDB 수 = 임베딩 수 | ko_scenes 38,450 + ko_slides 24,064 = **62,514** | ✅ (=#3) |
| 5 | features = 씬 수, 0 고아 | 36,291행 / 455작품, 고아 work_id **0** | ✅ |
| 6 | ChromaDB 한국어 정상(이전 깨짐작) | 궁01 씬 903건, **한글비율 0.80~0.85** | ✅ |

**핵심 등식**: 청크(62,514) = 임베딩(62,514) = ChromaDB(62,514). 3계층이 정확히 일치 → 손실·중복·고아 없음.

## 세부

### [1] 텍스트 무결성
456편 전수 스캔, 한글비율<10%(=모지바케 신호) **0편**. 이전 손상 112편 전부 정상화. 텍스트 손실 없음(재변환은 인코딩만 교정, 내용 보존).

### [2~4] tri-store 정합
- **청크**: 455작품 jsonl, 62,514행.
- **임베딩**: emb_cache 313샤드(워크스페이스 durable). emb2_new(샌드박스 재임베딩본)과 **md5 전수 일치(0 불일치)** — durable swap 완결 입증. 총 62,514 벡터.
- **ChromaDB**: ko_scenes 38,450 + ko_slides 24,064 = 62,514. 임베딩과 정확히 동수.

### [5] SceneFeature
36,291행 / 455 work_id. features work_id ⊆ 코퍼스(고아 0). ※ ko_scenes 38,450 vs features 36,291의 차(2,159)는 피처 산출 불가 단문씬(필터) — 설계상 정상, 고아 아님.

### [6] 한국어 정상성 (회귀 방지)
이전 깨짐작 **궁01**을 ChromaDB에서 직접 추출 → 903 씬 전부 정상 한글(비율 0.80~0.85). 모지바케가 벡터스토어까지 전파되지 않았음을 실증.

## 미변환 1건 (정직 기록·손상 아님)
- `연애의목적.hwp`(HWP5) → hwp5txt 추출 실패로 **txt 0바이트**. chunks/임베딩/ChromaDB에 **미포함**(455작품 카운트 밖). 인코딩 깨짐이 아니라 변환 실패 — 도구 보강 또는 재추출 과제. `fix_encoding.py` idempotent 검증 시 fixed=0/skipped=455로 확인됨.

## 잔여(과제 아님·설명됨)
