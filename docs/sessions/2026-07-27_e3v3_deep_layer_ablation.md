# E3-v3 실측 보고 — 심층 계층 주입 절제 (후향 정합, 2026-07-27)

**기준선**: HEAD `2a4086a` (E3-v2: 후향 정합 채택, 생성 기준선 4/12) · 동일 12항목·동일 판정(gpt-4o·후향 정합)

## 1. 결과 (절제 3조건)
| 조건 | 생성 프롬프트 | 후향 정합 (생성 선택) |
|---|---|---|
| v2 기준선 | 브리프+직전 시퀀스만 | 4/12 (33%) |
| v3 | +심층 계층(CharacterArc·복선 장부) **+"예상 밖 전개 감행" 지시** | 3/12 (25%) |
| **v3b** | +심층 계층만 (지시 제거) | **5/12 (42%)** |

## 2. 정직한 판정
1. **심층 계층 주입 효과: 미확정(방향 양수)** — 4→5는 n=12에서 노이즈 범위. 사전등록 원칙대로 "상승 확인"을 선언하지 않는다. 확정에는 표본 확대(작품 6개·k지점 확대로 n≥48) 필요.
2. **"예상 밖 전개 감행" 지시는 역효과(3/12)** — 놀라움을 지시로 강제하면 실제 미래와의 정합이 깨진다. 실제 작가의 도약은 무작위가 아니라 심긴 재료의 회수이기 때문. 프롬프트 지시로 창작성을 흉내 낼 수 없다는 국소 증거.
3. **방법론 자기 교정 기록**: v3 설계는 주입과 지시, 변수 2개를 동시에 바꾼 절제 위생 위반이었다 — v3b로 분리해 교정했다. 이 교정 과정 자체를 남긴다(사전등록·단일 변수 원칙 재확인).

## 3. 다음
① E3-v4: 표본 확대(n≥48, 작품·지점 확대)로 심층 주입 효과 확정 ② 주입 형식 개선 후보: 복선 장부를 "회수 기한·잔액" 형태로 구조화(현재는 목록 나열) ③ 병렬: DR-1 확산·DR-4 acclaim(사용자 저비용 트랙).

**문서 ID**: LOS-E3V3-DEEP-ABLATION-V1.0-2026-07-27 · state: e3v3_state.json·e3v3b_state.json 로컬 전용
