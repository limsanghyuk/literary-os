# GPT 한국드라마04 산출물 정본 편입 + 규격 일치화 1차 (2026-07-11)

이 폴더는 GPT가 저작한 SeqCard 산출물을 `seqcard_ko/` 정본에 **이중 게이트**로 편입한 작업과, 향후 GPT와의 **분석 규격 일치화 1차 제안**을 담는다.

## 무엇을 했나 (한 줄)

GPT 한국드라마04 파일럿 **4편**을 정본에 편입(net-new 3 + 시티헌터 GPT-완본 교체), 두 게이트 모두 **ERRORS 0**. 그리고 GPT에게 전달할 규격 일치화 1차 제안서를 작성.

## 편입 결과 (실측)

| 작품 | 회차 | 씬 | 시퀀스 | 밀도 | 편입 방식 | strict | new_layers |
|---|---|---|---|---|---|---|---|
| 결혼못하는남자 | 16 | 1250 | 189 | 0.151 | net-new | ERRORS 0 | ERRORS 0 |
| 101번째프로포즈 | 15 | 1125 | 184 | 0.164 | net-new | ERRORS 0 | ERRORS 0 |
| 공주가돌아왔다 | 16 | 1117 | 160 | 0.143 | net-new | ERRORS 0 | ERRORS 0 |
| 시티헌터 | 20 | 1356 | 171 | 0.126 | GPT-완본 교체 | ERRORS 0 | ERRORS 0 |

시티헌터: 구 정본(Stage1/2/3, 그래프층 없음, 구포맷 `_series_arc.json`)을 GPT 완본(6계층 + 그래프)으로 교체. 구 `시티헌터_series_arc.json` 폐기. GPT판 실측: edges 518(cross 55)/chararc 156/relarc 153/payoff 140.

## 파일

| 파일 | 내용 |
|---|---|
| `SPEC-ALIGNMENT-1차제안_v1.md` | **GPT에게 전달할 규격 일치화 1차 제안서** — 방법·깊이·밀도·규격/형식·검증 방식 일치 + 추가 계층 논의 |
| `비교평가_수정지시_수용안_v2.md` | 편입 전 비교평가·수정지시·수용안 (선행 보고서) |
| `GPT_vs_Claude_비교평가_v1.md` | 최초 비교평가 (v1) |
| `transform.py` | 편입 스테이징 변환기 (turning_point 정규화 + 브리지 엣지 분리) |
| `INGESTION_PIPELINE.md` | 편입 파이프라인·게이트 실행법·실측 결함 규약 |

## 실측된 형태 결함 4종 (편별 상이, 콘텐츠는 무손실)

1. `episodearc.turning_point` 형태 불일치(string / `{scene_no}` / `{seq_index}` 혼재) -> 게이트 크래시. 정규화 필수.
2. `seqblueprint` work_id 맨키(`{작품}`) -> SEQ work_id FK FAIL. `{작품}_{NN}` 정규화 필수.
3. 브리지 엣지(gap!=0)가 local 파일 혼입 -> 게이트는 통과하나 위생상 cross 파일로 이동(무손실).
4. `lx` 접두 edge_id -> 게이트 통과(발번포맷 미검사), 무해.

## 다음 단계

GPT의 나머지 한국드라마04(총 37편 예정) 산출은 위 규약을 재사용하여 편입. 워킹맘·최강칠우·카인과아벨 3편은 기존 정본과 중복 -> 충돌 해소 필요(파일럿 시티헌터와 동일 처리 판단). 규격 일치화 2차 라운드는 추가 계층 ablation 검증 후 진행.
