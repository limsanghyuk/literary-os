# E2-F 실측 보고 — 프런티어 생성 스모크: 브리프 부가가치 절제 + 필적성 탐지 (2026-07-27)

**기준선**: HEAD `af023ed` · **전략 전제(개발자 확정)**: 최종 생성기는 로컬 8B가 아니라 프런티어 LLM(Claude/GPT) — 우리 모델의 정체 = 프런티어를 지휘하는 구조 지능층(기획·구성·검증). 본 실험은 그 구도의 첫 실측.
**설계(사전등록)**: held 작품(시티헌터·오나의귀신님, 학습 미사용) 원문 씬 20개. 생성 gpt-4o-mini temp0.8 × 2군 — **A(우리 브리프 조건)** vs **C(소제목만)**. 판정 gpt-4o temp0·위치무작위·강제선택. 총 100콜, 비용 <$1.
- J1/J2(필적성·탐지 질문): "어느 쪽이 실제 방영 대본 발췌인가?" — A vs 원문 / C vs 원문
- J3(구성 충실도·특성 질문): "브리프 의도에 더 충실한 쪽은?" — A vs C

## 1. 결과 (있는 그대로)
| 판정 | 결과 | 해석 |
|---|---|---|
| **J3 브리프 충실도 (A vs C)** | **20/20 A 완승** | **코퍼스 브리프의 조향력 완전 실증** — 구성 지능이 프런티어 생성을 의도대로 이끈다 |
| J1 브리프 생성의 원문 통과율 | **0/20** | 판정자가 매번 원문을 골라냄 — 브리프만으로는 필적 불가 |
| J2 무브리프 생성의 원문 통과율 | 1/20 | 동일 |

## 2. 이 결과가 답하는 것 — 개발자의 질문에 대한 실측 답변
**"작은 모델로 인간 작가처럼 되겠는가"라는 의문은 옳다. 그런데 오늘의 발견은 더 나아간다: 프런티어 모델조차 브리프만으로는 아직 안 된다 (0/20).** 즉 격차의 본질은 모델 크기가 아니라 **시스템**이다. 분해하면:
1. **구성(무엇을 쓸 것인가)** — 해결됨: 20/20. 우리 브리프는 프런티어 생성을 의도한 사건·기능·장소로 정확히 조향한다. 코퍼스의 부가가치가 프런티어 위에서 처음 정량 입증됐다.
2. **표면 질감(어떻게 쓰인 것처럼 보이는가)** — 미해결: 생성 대본은 '대본 형식의 유려한 산문'이고 실제 대본은 압축·비정형·현장 지시의 고유 질감을 갖는다. 판정자는 이 질감으로 100% 구별한다.
→ 우리 시스템의 다음 지렛대가 정확히 이 지점을 겨냥하고 있다: **few-shot 원문 예시(corpus_ko ChromaDB RAG)** + **문체 프로파일(StyleDNA)** + **상위 생성기(gpt-4o/Claude)** + **게이트 재생성 루프**. 브리프(구성)는 이미 이겼으므로, 남은 것은 질감 주입이다.

## 3. 아키텍처 확정 함의
- 최종 구도 = **우리 구조 엔진(기획카드→아크→시퀀스→씬브리프 + 쌍대 게이트) × 프런티어 생성기**. 허브 모토 "판단은 로컬, 생성만 LLM" 그대로.
- 4070/8B 트랙의 재정의: 작가 후보가 아니라 ①구조 감각 학습 가능성의 실증 수단(R1·R2 ΔW) ②저비용 로컬 심판/가드 후보 ③프라이버시·비용 폴백. E2 로컬 라운드는 이 역할 안에서 계속 가치가 있다.
- 프런티어는 가중치 학습 불가 → 우리 학습 신호는 **컨텍스트(브리프·RAG·프로파일)와 게이트 루프**로 주입된다. 코퍼스가 곧 모델이다.

## 4. 한계 (정직)
- n=20·생성기 4o-mini(프런티어 최하급)·판정자 단일(gpt-4o). '탐지 질문'은 필적성의 보수적(엄격) 기준 — 인간 시청자 기준과 다를 수 있음.
- J3 20/20은 조향력 증명이지 품질 증명이 아님(브리프 충실≠좋은 씬). 품질 축은 J1 계열이 담당.

## 5. 다음 (E2-F2 사전등록 초안)
동일 20씬에서: ①생성기 gpt-4o(또는 Claude) ②few-shot: 같은 작품 인접 원문 2씬 예시(RAG) ③StyleDNA류 문체 지시. 지표: J1 통과율 0/20 → **≥4/20**이면 질감 주입 경로 유효 판정. 이후 사다리: 통과율을 회차 단위·인간 판정으로 상향.

**문서 ID**: LOS-E2F-FRONTIER-SMOKE-V1.0-2026-07-27 · 재현: /tmp/e2f/run.py(세션 산출물, state.json 동봉 — verbatim 포함이라 로컬 전용)
