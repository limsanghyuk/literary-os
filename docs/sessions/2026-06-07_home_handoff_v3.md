# 회사→집 연속 핸드오프 v3 (2026-06-07) — 최신 종합
내일 집 로컬에서 이 문서 하나로 이어받기. (선행 v2는 구버전, 본 v3가 정본)

## 0. 한 줄 / 진입점
장기 로드맵·기획 누적 단계(공식 개발 아님). 설계 거의 완비 + 핵심 검증 POC 다수.
진입: `docs/sessions/INDEX.md`(정본 읽는 순서) → `docs/README.md`(저장소 지도) → 본 문서.

## 1. 듀얼 트랙 주의 (혼동 금지)
- **Claude 트랙(우리 작업)** = Literary OS / Sovereign OS, V745, 공식 F-01~F-55.
- **GPT 트랙** = V1700 / Stage184(별 계보, 흡수 후보 참조만).
- 공식 검증·데이터 작업은 Claude 트랙 기준.

## 2. 작업 성격
대화를 누적·정리·발전 → 미흡 알고리즘·기획 보강 → Phase E~G+ 장기 로드맵·제안서·설계도 구체화.
중간 실행물(MVE·corpus_migrate·공식 하니스·트라이스토어)은 '개발'이 아니라 설계를 근거화하는 **검증 POC**.

## 3. 전체 누적 흐름
1) 진단: V745 무결성 결함(SHA256SUMS stale)→SP-E.0 핸드오프. 로컬 zip 정리.
2) 기획: Phase E~G 통합 + 검증 우선 입구 관문(PROXY-MVE 무승부+길이교란).
3) 아키텍처(3인 합의): 엔진=도메인특화 에이전트 오케스트레이션, 공식=영속 검증 타입시스템, LLM-0→2.5 점진교체.
4) 허브 체계화: INDEX·docs/README·재배치.
5) 데이터: V21 179편→데이터모델 v2(L0~L4)→L0 시드 178편 실적재(corpus_migrate).
6) 코어 알고리즘 6 보강(의사코드): G7 생성오케·G4 NKG·G5 Critic5축·G9 RAG·G6 학습루프·G2 narrative_analyzer.
7) 데이터 적정성: L0 부족 진단(공식은 씬단위 정량 요구)→보강계획→L2/DRSE/L3 파일럿→실 공식 계수학습 실행.
8) 공식 전수조사: ~25개 순수공식 인벤토리 + 검증 로드맵(통합 하니스 Stage0~6).
9) 공식 검증 실행: fitness 상관 0.40→재가중 0.59 / DRSE TFIDF 0.02→임베딩 0.71(결함 개선 실증).
10) 데이터 아키텍처: 두 방식(벡터 ChromaDB / NKG 그래프) + 신규 ③ 피처테이블 = 트라이스토어. 소싱 A(공개 구조분석)/B(합법 스크립트). 트라이스토어 5편 시범적재 동작.

## 4. 핵심 실측 결과 (모음)
| 항목 | 결과 |
|---|---|
| V745 무결성 | SHA256SUMS 해시불일치 다수(매니페스트 미재생성)=진입 선결 SP-E.0 |
| PROXY-MVE | 단일씬 구조프록시 vs 순수 LLM = 무승부+길이교란(얕은 프록시 무의미) |
| fitness 공식 | 가중치 미정렬(종합0.40<motif0.82) → 재가중 ρ 0.40→0.59 |
| DRSE 잔향 | TFIDF 0.02(실패) → 임베딩 0.71(복선 포착, 임베딩 전환 필요) |
| 공식 인벤토리 | ~25개 순수공식 거의 전부 실데이터 타당성 미검증, 일부 합성 의존 |
| 트라이스토어 | 벡터+그래프+피처 5편30씬 적재·질의 동작(절정 avgQ9.6 아크) |

## 5. 정직한 현재 위치
- 설계: 코어 알고리즘6 + 데이터모델 + 트라이스토어 + 공식 검증 로드맵 = 거의 완비.
- 미완(개발 모드): SP-E.0 무결성 실행, critic/·orchestration/ 신규모듈, 데이터 규모적재(회차 L2/L3 30~50편), 인간 작가 GT 검증.
- 검증: proxy(LLM) 단계. 절대 타당성=인간+규모(G_VALUE_PROOF).

## 6. 집에서 이어받을 다음 순서 (우선)
1. 데이터: Gold 후보 합법 스크립트 가용성 조사(경로B) + 공개 구조분석 확대(경로A) → 트라이스토어 30~50편.
2. 공식 검증 Stage 후속: DRSE BGE-M3 실모델 전환, fitness 재가중 held-out, Stage3~6(longform·trajectory·NIE·종합).
3. SP-E.0 무결성 실행(진입 선결, 개발).
4. 코어 모듈 구현 착수(critic/·orchestration/, 개발).

## 7. 재현 도구·데이터·키
- 공식 검증 하니스: tools/formula_validation/harness.py
- 트라이스토어 빌드: tools/tri_store/build_tristore.py
- 코퍼스 정규화: tools/corpus_migrate.py
- 데이터: data/corpus_seed/ (L0시드178·L1 POC·파일럿L2·검증18씬·5편30씬)
- 키: 환경변수 전용(노출0). Anthropic·OpenAI 잔액소진, Gemini 사용가능(gemini-2.5-flash 생성, gemini-embedding-001 임베딩).
- Drive 공식폴더: 로그인 필요로 미접근(공개전환/업로드 시 분석).

## 8. 협업 원칙
매 세션 누적·정리·발전, 항상 허브 로드 → 회사·집·미래 협업자 연속.
