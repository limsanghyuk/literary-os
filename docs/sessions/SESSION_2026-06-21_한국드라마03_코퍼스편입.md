# 세션 핸드오프 — 2026-06-21 · 한국드라마03 코퍼스 편입 (집 이어작업용)

> **목적:** 회사에서 한국드라마03을 corpus_ko에 증분 편입함. durable 레이어(텍스트~NKG)는 완료.
> 집 로컬에서 임베딩+ChromaDB+features 풀런으로 마무리하면 됨. 본 문서가 그 인수인계서.

---

## 0. 한 줄 요약
corpus_ko **2,030 → 2,242 works** (+212, +11,656 scenes). scenes/chunks/features(어휘)/NKG/manifest **반영 완료**.
**임베딩·ChromaDB·features(motif/curiosity)만 집에서 1회 풀런 필요** = 신규 212편 한정.

---

## 1. 물리 저장소 (중요)
- **본체 경로:** `C:\Users\inyoung pharm\Documents\Claude\Projects\literary\corpus_ko`
  - = 샌드박스 마운트 `/sessions/<id>/mnt/literary/corpus_ko` (동일 폴더, 별도 저장소 아님)
  - 신규 데이터는 이 경로의 기존 구조(`txt/ scenes/ chunks/ features/` + `manifest.json nkg.json`)에 **그대로 증분**됨.
- **소스 아카이브:** `C:\claude\Scripts\한국드라마03` (22개 아카이브)
- **보고서/핸드오프:** `C:\claude\claude\`

---

## 2. 지시와 이행
> "한국드라마03 조사 → 추가 드라마를 이전 대본과 비교 → **새로운 대본만** DB 작업 → **기존 드라마는 삭제**"

| 요구 | 이행 |
|---|---|
| 신규/기존 판별 | 제목 정규화 dedup → 신규 20 / 중복 2 |
| 기존(중복) 삭제 | 내여자친구는구미호·파스타 **소스 아카이브 삭제 ✅** |
| 신규만 DB 작업 | `parse_incr.py`로 신규 work_id만 처리, 기존 2,030 비파괴 보존 |

---

## 3. 단계별 실측 결과

### 3.1 dedup (22 아카이브)
- 신규 20편 / 중복 2편(내여자친구는구미호, 파스타)
- zip 14편은 추출·변환 성공 → 212 works 편입
- **egg/alz 6편은 미추출**(아래 §6-B)

### 3.2 텍스트 변환
- zip 해제 → 포맷 라우팅: hwp→hwp5txt, pdf→pdftotext, docx→python-docx, txt→인코딩추정(utf-16/cp949/utf-8)
- 산출: `txt/{work_id}.txt`, work_id 포맷 = `{정규화드라마명}_{회차2자리}` (예: `스카이캐슬_02`)

### 3.3 씬 파싱 + 듀얼 청킹 — `parse_incr.py`
- parse.py의 `build_scenes()`/`sliding()` **로직 그대로 복제**, 신규 work_id만 처리
- `parse_stats.json` **비파괴 병합**(기존 2,030 항목 보존) — raw parse.py 실행 시 기존 항목이 `("skip",0)`로 덮이는 버그 회피
- 산출: `scenes/{wid}.jsonl`(work_id,scene_no,heading,text,method), `chunks/{wid}.jsonl`(scene청크 + `kind:"slide"` 전문 슬라이딩청크)
- 결과: 212 works, 11,656 scenes (min/med/max 3/60/98)

### 3.4 NKG — `nkg.py` (모델 불요, 전체 재빌드)
- 2,242 works · scene-nodes 134,337 · NEXT 132,095 · 인물 14,121 · char-scene 118,257
- 샘플 검증: 올드보이/마더/곡성 인물·co-occurrence 페어 정상

### 3.5 features (어휘 피처) — `feat_incr.py`
- 신규 212편에 conflict_intensity·scene_energy_ratio·dialogue_ratio·n_chars 등 작성
- **motif_residue_score·curiosity_gradient = 0 (임베딩 의존 → 집에서 채움)**
- features/ = 2,242 (전수)

### 3.6 manifest
- stale count(455, 2026-06-13 고정) → 실측 **2,242** 정정
- `last_increment` 블록에 이번 배치 기록(신규 212, 드라마 목록, pending 항목, 캐비엇)

---

## 4. 무결성 검증 (실측)
- 신규 212 works: scenes+chunks+features 전수 존재·비어있지않음·JSONL 파싱 정상 → **이슈 0**
- slide-chunk 전문 커버리지: worst case(여우각시별_16)도 **124%** → 텍스트 손실 0
- 기존 2,030 features 미손상(네멋대로_11 motif 57/58 유지 확인)
- emb_cache 실측: distinct work_id **2,030** = 한국드라마02 포함 기존 코퍼스 전부 임베딩됨

---

## 5. ⚠️ 캐비엇 — 텍스트 손실 0, scene 분절만 거침
여우각시별 다수·스카이캐슬·눈이부시게 등 **~25편**은 헤딩 포맷이 슬러그(`장소. N./D.`) 또는
도입부 recap-montage(`1.~5.`)라 scene 경계가 거칠게 잡힘.
→ **전문은 `kind:"slide"` 청크로 124% 완전 보존** → 임베딩/RAG 손실 0, scene-level feature 입도만 영향.
→ 기존 2,030과 **동일 parse.py 기준**이라 일관성 유지. (개선하려면 슬러그 regex에 N./D. 추가 후
   전체 재파싱 필요 = 2,030 영향 = 별도 의사결정. 이번엔 "신규만" 지시 준수 위해 미적용.)

---

## 6. 🏠 집 로컬 — 남은 작업 (정확한 절차)

### (A) 임베딩 + ChromaDB + features 풀런 — **필수, 신규 212편 한정**
```bash
cd C:\Users\inyoung pharm\Documents\Claude\Projects\literary\corpus_ko
set OPENAI_API_KEY=<키 재주입>           # 임베딩용 (사용 후 env 제거됨)

python embed_par.py        # 신규 chunk만 append-only shard 추가 → emb_cache 2030→2242 커버
python store_chroma.py     # 전체 emb_cache로 ChromaDB 재빌드 (로컬 fs, FUSE sqlite 회피)
python features.py         # 전체 재실행 → 신규 212편 motif/curiosity 0→실값
```
- **한국드라마02는 재임베딩 불필요** (이미 emb_cache 2,030에 포함).
- `features.py`는 emb_cache 1.3G 로드 때문에 샌드박스 45s 한도 초과 → 집에서 1회 풀런이 정규 경로.
- ChromaDB는 원래 샌드박스 휘발 인덱스 → 집 `store_chroma.py` 재빌드가 정규 경로(88% 실증 이력).

### (B) egg/alz 6편 추출 — 샌드박스 도구 부재
ALZip 독점 포맷, 샌드박스에 unar/unalz/root 없음. 집에서 ALZip(또는 Bandizip) 해제 후
§3.2~3.5 파이프라인 재적용:
```
C:\claude\Scripts\한국드라마03\:
  구해줘.Egg · 달이뜨는강.Egg · 데릴남편 오작두.Egg · 스위치.Egg · 우아한가.Egg · 바람의화원.alz
```

### (C) hwp3 잔여 소수 — hwp5txt/soffice 미지원
밤을걷는선비·여우각시별 일부 hwp3(구포맷). 한글에서 hwp5/docx 재저장 후 변환 재시도.
(대부분 회차는 이미 변환 성공, 잔여 소수만 해당)

---

## 7. 신규 편입 15개 드라마 (work_id 수)
구르미그린달빛(19) · 굿캐스팅(16) · 그대웃어요(4·합본범위) · 녹두꽃(24) · 눈이부시게(10) ·
밤을걷는선비(10) · 배가본드(16) · 백일의낭군님(15) · 스카이캐슬(20) · 스토브리그(17) ·
여우각시별(15) · 질투의화신(24) · 해품달(9) · 황진이(13) · 연애의목적(1)

---

## 8. 사용한 보조 스크립트 (재현용, 샌드박스 임시 — 집엔 불필요)
- `parse_incr.py` — 신규 work_id만 씬파싱+청킹, parse_stats 비파괴 병합
- `feat_incr.py` — 신규 work_id 어휘 피처(motif/curiosity=0)
- 정규 스크립트(집에서 사용): `embed_par.py`, `store_chroma.py`, `features.py`, `nkg.py` (corpus_ko 내 기존)

## 9. 산출 위치 요약
| 산출물 | 경로 | 상태 |
|---|---|---|
| txt/scenes/chunks/features | `corpus_ko/` | ✅ durable 반영 |
| manifest.json / nkg.json | `corpus_ko/` | ✅ 갱신 |
| emb_cache (신규 212) | `corpus_ko/emb_cache/` | ⏳ 집 embed_par |
| ChromaDB | 로컬 fs | ⏳ 집 store_chroma |
| features motif/curiosity | `corpus_ko/features/` | ⏳ 집 features.py |
| 본 핸드오프 | `C:\claude\claude\SESSION_2026-06-21_한국드라마03_코퍼스편입.md` | ✅ |
