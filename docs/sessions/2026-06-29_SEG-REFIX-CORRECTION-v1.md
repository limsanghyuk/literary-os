# ★정정 — 씬 분할 결함 스캔 오류 수정 (2026-06-29)

선행: 2026-06-29_SEG-REFIX-REPORT-v1 의 일부 결론을 **정정**한다.

## 무엇이 틀렸나
초기 스캔이 씬 마커를 **`#N`만** 셌고, 한국 대본이 혼용하는 **`S#N`** 마커를 빠뜨렸다. 그 결과:
- 대장금 1화를 "52씬, #7~19 원본 누락(source-incomplete, 재입수 필요)"로 **오판**.
- 사용자가 원본(`Scripts/한국드라마02/대장금/..._ep01.txt`, EUC-KR)을 직접 풀어 반증.

## 실제 (통합마커 `(?:S#|#|씬)\d+` 재검증)
- **대장금 1화 = 69씬, 범위 1~69, 누락 0.** #7~19는 `S#7`~`S#19`로 실재.
- 즉 "원본 누락"이 아니라 **복구 가능한 파서 결함**(마커 혼용 미처리)이었다.
- 깨끗한 변환본 `corpus_ko/txt/대장금_01.txt`에도 69씬 전부 존재.

## 정정된 결함 지도 (888회차, 통합마커 기준)
- **복구 가능 결함 = 56회차** (이전 "69·source-incomplete 포함"은 오류).
- 분포: **천만번사랑해 53 · 대장금 2 · 동이 1**. 거의 전부 천만번사랑해(소문자 `s#N.` 미처리).
- 확인된 "원본/​txt 누락"은 없음(스캔 대상 중). txt 미생성(스위치·바람의화원)은 변환 재실행 사안으로 별개.

## 조치
- 재파서를 **통합 마커**로 교체(`tools/seg_refix`). 천만번사랑해 110 + 대장금 54 + 동이 재파싱 → `scenes_refix/`(대장금 ep01=69씬, 평균 68.5).
- 교훈: 한국 대본 씬 마커는 `#N`·`S#N`·`s#N.`·`씬N` 혼용 → 파서·스캐너는 항상 통합 매칭. 거짓양성(연도/타임코드)은 최대#>300 필터.

## 사과/자기점검
사용자 지적이 정확했다. 미선언 금지 원칙대로 단일 마커 가정을 검증 없이 결론에 반영한 것이 오류. 정정 반영.
