# 씬 분할 결함 전수 스캔 + 재분할 보고 (① , 2026-06-29)

- verbatim 원문 비커밋 원칙 유지 — 본 보고는 도구·집계만. 데이터는 로컬 db 사이드카 `corpus_ko/scenes_refix/`.
- 도구: `tools/seg_refix/scan_and_refix.py`

## 1. 결함 스캔 결과 (드라마 103편 / 1,084 회차)
- **진짜 결함 69회차(6%)** — 저장 씬수 ≪ 헤딩 최대번호 + 갭.
- 거짓양성 9회차 = 헤딩의 연도/타임코드를 씬번호로 오인(예: 38사기동대 "최대#11738", 뉴하트 "#1333") → 최대#>200 필터로 제외.
- 결함 집중: **천만번사랑해 40 · 성균관스캔들 5 · 스위치 4 · 선덕여왕 3 · 한반도 3 · 대장금 2 · 동이 2 · 베토벤바이러스 2 · 기타 단발**.

## 2. 근본 원인 3분류 (대장금/천만번사랑해 실측)
| 유형 | 사례 | 복구 |
|---|---|---|
| **파서 마커 누락** | 천만번사랑해(마커 소문자 `s#N.`인데 파서가 `S#/#`만) | ✅ 재파싱으로 복구 |
| **원본 txt 자체 누락** | 대장금 1화(txt에 #7~19 부재, 최대 #69지만 52씬만 존재) | ❌ 원본 재입수 필요 |
| **txt 미생성** | 스위치_14·바람의화원_06(txt 없음) | ❌ 변환 재실행 필요 |

## 3. 재분할 실행 (최대 결함작)
- **천만번사랑해 110회차 재분할 완료** — `s#N.` 마커로 재파싱, **평균 57.5씬/회차**(이전 6~22 → 정상 50~69). 사이드카 `scenes_refix/`.
- 효과: 단일 작품에서 110회차 × ~50씬 ≈ 6,000+ 씬이 제대로 분할됨(이전 ~1,500 손실분 회복).

## 4. 남은 결함 처리
- 파서 마커 누락형(성균관스캔들·선덕여왕 등): 각 작품 txt 마커 확인 후 동일 재파싱(다음 배치).
- 원본/​txt 누락형(대장금1·스위치·바람의화원): 개발자가 원본 재입수/재변환해야 함(플래그).

## 5. 다음(②)
재분할된 정상 씬에서 **씬별 소제목+설명(SceneBrief)을 Claude로 직접 추출**(GPT 미사용). 결함 회차는 ② 대상에서 보류.
