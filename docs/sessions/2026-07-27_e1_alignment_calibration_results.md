# E1 실측 보고 — 정렬기 골드셋 캘리브레이션 + 대장금 6회 하이브리드 파일럿 (2026-07-27)

**트랙**: DR-2 AlignmentLayer (로드맵 v1.1r §5·§9.5 E1, 1순위) · **기준선**: HEAD `0ab7fe0` · **방법**: 결정론·LLM-free(비용 $0) · 재현: `align_scenes_e1.py`
**골드셋**: corpus_ko/brief2prose **479쌍**(대장금 EP01~06 + 천만번사랑해 EP01) — AM-4 지시대로 정렬기 채점에 전용.

## 1. 사전등록 (실행 전 고정)
- H-E1a (블라인드 폴백): 브리프(소제목·설명·인물)만으로 정답 씬 top-1 탐지 — 측정치 자체가 산출물(임계 사전등록용).
- H-E1b (하이브리드 실전): 카드 heading 번호+텍스트 검증으로 정렬률 **≥90%** (DoD).

## 2. 결과 (있는 그대로)
| 시험 | 결과 | 판정 |
|---|---|---|
| E1a 블라인드 의미 정렬 (479쌍) | top-1 **80.8%** (작품별 66.7~93.2%) | 폴백 하한 확정 |
| E1a 임계 스윕 | conf≥0.5에서 정밀도 87.1%/커버리지 45.3% — conf 단독 분리력 약함 | 폴백은 수동큐 병행 필수 |
| **E1b 하이브리드 (대장금 6회, 카드 426장)** | **426/426 = 100.0%** (EP별 전부 만점) | **PASS (DoD +10%p 상회)** |
| 파서 정합 | 파싱 씬 수 = 골드 쌍 수, 7/7 에피소드 정확 일치 | 분할기 신뢰 확인 |

## 3. 발견·교정 1건 (정직 기록)
- 1차 실행에서 EP02 30건 "미정렬" — 원인은 정렬 실패가 아니라 **카드 heading 표기 변형**("S#15" vs "#15")을 번호 파서가 못 읽은 버그. `[sS]?#?\s*(\d+)` 교정 후 100%. → 표기 변형은 실재하므로 파서 패턴을 정본 스키마에 명기할 것.

## 4. 임계 사전등록 (본 실측으로 확정 — 이후 변경은 별도 커밋+사유)
1. **헤딩 모드(1차)**: 번호 일치 + 헤딩 bigram 유사도 ≥0.5 → 자동 정렬 (실측 최저 유사도 0.667로 여유 확보). 카드에 heading이 있는 정독 저작 작품은 이 모드로 충분.
2. **의미 폴백(2차)**: 헤딩 부재/불일치 시 — conf ≥0.5만 자동 수확(정밀도 87%), 미만은 `_MANUAL_QUEUE`. AM-6 안전마진(하위 10% 수확 제외) 유지.
3. **카디널리티**: 본 파일럿에서는 1:1 완전 대응(카디널리티 예외 0건) — 단 대장금은 정독 저작이 원문 헤딩을 보존한 경우. GPT 직접독해 재구성 작품(위치헤딩, 원문 번호 미인용)은 폴백 모드 대상이며 별도 파일럿 필요.

## 5. 함의 — DR-2 본격 가동 조건 충족
- 저작 카드가 원문 헤딩을 보존한 작품군(Claude 정독 계열)은 **비용 0·정확도 ~100%로 즉시 대량 정렬 가능** → brief2prose 수확 파이프 개통.
- 다음 순서(로드맵 §9): ①정렬 확대(Claude 정독 작품군 일괄) → ②수확 harvest_pairs 가동(1차 목표 ≥10,000쌍) → ③GPT 재구성 작품군 폴백 파일럿 1작. 병렬: DR-1 파일럿(대장금 기획층).

## 6. 자기 점검
- 골드셋 7에피소드는 전부 동일 파이프라인 출신 — 외부 일반화(타 파서 산출·hwp 변환 편차)는 확대 시 재측정. 스팟체크 100건(§5 DoD)은 확대 단계에서 실행.
- E1a의 80.8%는 폴백 하한이지 실전 성능이 아님(실전은 E1b 경로 우선). 이 구분을 흐리지 말 것.
- 대장금 EP02~06 seqcard 카드가 골드 쌍 수와 동일하다는 사실 자체가 seqcard 저작이 원문 씬 구조를 보존했다는 독립 증거(69/88/61/74/63/71 전부 일치).

**문서 ID**: LOS-E1-ALIGN-CALIB-V1.0-2026-07-27 · 다음: DR-2 확대 + DR-1 파일럿(대장금)
