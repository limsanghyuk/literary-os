# E2 라운드2 실측 보고 — 약열화(L2) 난이도 상향 (4070, 2026-07-27)

**기준선**: HEAD `0d78641` (R1 보고) · 재현: `db/4070_oneclick/E2_R2.bat` + `e2r2_pairs_{train,held}.jsonl` + `e2r2_result.txt` · 실행: R1과 동일하게 Claude 자율 수행

## 1. 설계 변경 (R1 대비 단 하나)
- rejected = 전면 셔플(L1) → **인접 행 스왑 ~22%(L2 약열화)**. 토큰 멀티셋·길이 동일 유지(길이 인공물 0 불변). 그 외 전부 동일(같은 250/60쌍 원천, 같은 held 작품 분리, 같은 킷·에폭).
- 사전 예측(R1 보고 §4): L2에서 W0가 내려가 헤드룸이 열리고, 학습의 진짜 값어치가 드러날 것.

## 2. 결과
| 지표 | R1 (강열화) | **R2 (약열화)** |
|---|---|---|
| HELD W0 (베이스 변별) | 0.950 | **0.850** (난이도 상향 확인) |
| HELD W1 | 0.983 | **0.933** |
| **ΔW** | +0.033 | **+0.083 (2.5배)** |
| 마진 M0→M1 | +60.9→+94.2 | +18.5→+24.7 (+34%) |
| KL/token | 0.0486 | **0.0187** (더 안정) |
| 판정 | ADOPT-cand. | **ADOPT-candidate** · `lora_e2_r2` |

## 3. 검증된 것 — 이번 라운드의 핵심 발견
1. **난이도 사다리 가설의 학습 축 실증**: "가중치·학습의 값어치는 박빙에서 가려진다"(3인 심의·H 절제의 예약 과제)가 판별이 아닌 **학습 이득**에서 확인됐다 — 쉬운 과제(L1)에선 ΔW +0.033, 어려운 과제(L2)에선 **+0.083**. 코퍼스 학습은 포화 구간이 아니라 미세 구조 감각 구간에서 더 크게 작동한다.
2. **미세 순서 감각의 학습 가능성**: 인접 행 22% 스왑은 사람도 정독해야 잡는 교란인데, 베이스 85% → 학습 후 **93.3%**(미학습 작품 기준). 브리프 조건의 국소 전개 순서 감각이 실 코퍼스 250쌍·1에폭으로 유의미하게 개선된다.
3. KL 0.019 — 난이도를 올렸는데 드리프트는 오히려 감소. 보상해킹 징후 없음.

## 4. 한계·다음
- R1·R2 모두 fresh-base 독립 라운드 — **누적 체이닝**(graduation v3 프로토콜)은 어댑터 로드 지원 스크립트 확장 후. 구조 게이트(c3류) 잔여 동일.
- L3(생성 초안 vs 원문) 라운드가 다음 사다리 — 생성 초안 제작에 LLM 호출 필요(OpenAI, cost_cap). 그다음 L4(생성 vs 생성 박빙).
- 동일 원천 쌍 재사용(R1과 같은 씬들) — R3부터는 미사용 수확분(잔여 ~23,000쌍)에서 신규 샘플링.

**누적 기록**: rounds_ledger.jsonl 2건 (R1·R2 모두 ADOPT-candidate, dW>0·KL PASS)
**문서 ID**: LOS-E2-R2-WEAKDEG-V1.0-2026-07-27
