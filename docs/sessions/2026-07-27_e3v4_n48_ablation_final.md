# E3-v4 실측 보고 — n=48 양팔 절제 확정: 심층 주입 무효(음성 결과) (2026-07-27)

**기준선**: HEAD `a1b6940` · 설계: 6작(시그널·모래시계·대장금·공주의남자·시티헌터·오나의귀신님) × EP04~07 × 2지점 = **48항목, 짝지은 양팔**(동일 항목에 A/B 모두 생성·판정). 생성 gpt-5.1 · 판정 gpt-4o 후향 정합(항목별 고정 시드).

## 1. 결과
| 팔 | 후향 정합 (생성 선택) |
|---|---|
| A 기준선 (브리프+직전 시퀀스) | **12/48 (25.0%)** |
| B 심층 주입 (인물 상태+구조화 복선 장부) | **9/48 (18.8%)** |
| 불일치쌍 | A만 승 6 vs B만 승 3 |

**판정: 심층 계층 주입은 후향 정합을 개선하지 않는다 — 방향은 오히려 음수.** v3b(5/12)의 상승은 소표본 노이즈였음이 확정. 절제 사슬 최종: v2 4/12 → v3 3/12 → v3b 5/12 → **v4 짝지은 48쌍에서 음성**.

## 2. 왜 안 오르는가 — 두 갈래 해석 (모두 기록)
1. **과거 정박 효과**: 복선 장부·인물 상태는 모델을 '심긴 재료의 연장'으로 정박시킨다. 그러나 실제 다음 시퀀스는 자주 **신규 재료를 개시**한다 — 주입이 과거 지향 매끄러움을 강화해, 실제 미래를 닻으로 삼는 판정에서 불리해진다. "예상 밖 전개 지시 역효과"(v3)와 합치면: **과거를 더 준다고 미래를 더 맞히지 않는다.**
2. **지표의 성격**: 후향 정합은 '실제 궤적 예측력'이지 '설계 품질'이 아니다 — 실제와 다른 좋은 설계도 감점된다. 따라서 본 음성 결과는 "주입이 품질을 해친다"가 아니라 "주입이 실제 궤적 예측을 돕지 않는다"로 한정해 읽는다.

## 3. 전략 전환 — 컨텍스트 추가가 아니라 선택과 학습으로
"더 많은 정보 주입"의 한계효용이 실측으로 0 이하로 확정됐으므로, 구조 성능의 다음 지렛대를 전환한다:
1. **E3-v5 (차기 1순위): best-of-n + 공식 선택** — 후보 설계 4개 생성 → 결정론 구조 점수(복선 회수 기대·긴장 연속·인과 정합)로 최선 선택 → 후향 정합 재측정. 공식이 R2(보상)가 아닌 **선택기**로 처음 배선되는 실험.
2. 구조 쌍대 학습: E2 로컬 트랙(4070)과 연결 — 시퀀스 설계 쌍(실제 vs 열화/생성)으로 DPO, 구조 감각의 가중치 학습.
3. 기획층 통합: WorkPlanningCard(작품 방향성)를 최상위 컨텍스트로 — 회차 내부 정보가 아니라 작품 수준 의도가 미래 예측에 기여하는지 별도 절제.

## 4. 자기 점검
- 양팔 절제·항목별 고정 시드·짝지은 비교로 v3의 위생 문제를 교정했다. n=48 불일치쌍 9로 McNemar 유의성은 없으나 방향 판단에는 충분.
- 새 4작 포함으로 전체 기준선이 33%→25%로 내려간 것은 회차·작품 난이도 혼합 효과 — 작품별 편차 분석은 표본 확대 시.
- 금일 API 비용 누계 ~<$15.

**문서 ID**: LOS-E3V4-N48-ABLATION-V1.0-2026-07-27 · state: /tmp/e3v4/state.json → 로컬 백업
