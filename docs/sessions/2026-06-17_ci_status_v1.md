# CI 상태·진단 보고 (2026-06-17) — ci_4tier 복구 + tier2 잔여

**기준**: 허브 `7c45df4`(V780/v13.33.0) · 트리거: CI #189~218 전부 실패(jobs=0) 조사.

## 1. 해결 완료 — tier1 녹색화 (근본 원인 + 실패 테스트)
- **★근본 원인(startup 실패)**: `ci_4tier.yml` 40행 `run:`의 `print(f'GATES: ...')` 콜론을 YAML이 매핑으로 오해 → 워크플로 파싱 실패 → **#189~218 전부 잡 0개**(테스트 미실행). 블록스칼라(`|`)로 수정(`4c29fe4`).
- YAML 수정 후 잡이 처음 실행됨 → 노출된 tier1 실패 5건 수정:
  - `test_tc25_list_providers` ==3→==4 (V767 LocalGPUAdapter, `75cdd3e`)
  - `test_v681_benchmark_p99` 4건: 없는 enum `GENERATE/ANALYZE`→`SLO_MONITOR` (`da9be83`)
- **결과: tier1-unit = PASS** (CI #220, 4,731 유닛 통과).

## 2. 잔여 — tier2-integration 4건 실패 (★개발자 도메인, 미수정)
YAML 복구로 tier2가 ~30런 만에 처음 실행되며 **기존 통합 무결성 실패**가 드러남:

| 테스트 | 성격 | 조치 주체 |
|---|---|---|
| `test_gate_count_80` | **stale 카운트**: 기대 80 vs 실제 GATES=97 | 안전 bump 80→97 |
| `test_g37_duplicate_zero_pass` | **실제 결함**: 중복 클래스 13건 | 아키텍처 dedup |
| `test_g61_phase_b_exit_gate_pass` | **★행(hang)**: SQLiteRealAdapter "near INVALID" 반복 → timeout>15s | 행 디버그 |
| `test_cross_g37_and_g61_both_pass` | 위 둘 의존 | 위 해결 시 자동 |

### G37 중복 클래스 13건 (이름 충돌 — dedup 또는 allowlist 필요)
`QualityLabel`(validation/gold_standard_builder ↔ quality/quality_labels) · `RoutingDecision`(llm_bridge/agent_envelope ↔ learning/provider_router) · `AuditRecord` · `CircuitState` · `StepStatus` · `HealthRecord` · `BridgeResult` · `ConflictType` · `AlertSeverity` · `TenantNotFoundError` · `PluginRegistry` · `ExitCheckpoint` · `GateCheckResult`. 
※ `QualityLabel`/`RoutingDecision`은 최근 V768/V775/V776에서 추가된 클래스가 기존과 충돌. **어느 쪽이 정본인지는 개발자 결정** → 무단 rename 금지.

### G61 행(hang) — CI 13분+ 블록 원인
`test_g61_phase_b_exit_gate_pass`가 SQLiteRealAdapter에 INVALID SQL 적용(롤백 테스트로 추정) 중 무한 대기. CI tier2 명령에 `--timeout` 없어 6시간까지 블록 가능(#220 816초 실측 후 취소).

## 3. 권고 (개발자)
1. **CI 하드닝(즉시·안전)**: ci_4tier.yml 각 tier pytest에 `--timeout=60`(pytest-timeout 이미 dev 의존). 행→빠른 실패로 6h 블록 방지.
2. **gate_count_80 → 97** 갱신(stale).
3. **G37 13중복 dedup**: 정본 모듈 결정 후 한쪽 rename(특히 QualityLabel·RoutingDecision은 신규 추가분 정리).
4. **G61 행 수정**: SQLiteRealAdapter INVALID-SQL 경로의 무한 대기 차단(타임아웃/예외).
- tier3(release gate)·tier4(preflight)는 로컬 PASS 확인됨 → tier2 통과 시 전체 녹색 예상.

## 4. 한계(정직)
G37 dedup·G61 행은 아키텍처 결정·런타임 디버그라 데이터/설계 트랙이 무단 수정하면 위험 → 진단·기록까지만. tier1 차단(YAML+5테스트)은 해소 완료. 최종 녹색은 tier2 4건 개발자 수정 후.
